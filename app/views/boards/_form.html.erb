<%= simple_form_for(@board) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :fen %>
    <%= f.association :move %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
